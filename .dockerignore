.git/
.idea/
.claude/
.DS_Store

# never ship runtime state or secrets
uploads/
build/
*.env
app.env

# frontend: node_modules/dist are rebuilt in the image; .env would bake the wrong
# VITE_BASE_URL (the container build passes it as a build arg instead).
interface/node_modules/
interface/dist/
interface/.env*

# docs / local
*.md
.todo
.draft
tests/testdata/
