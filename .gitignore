# build
bin

# MacOS
.DS_Store

# VSCode
.vscode

# env config
.env
app.env

# local-only compose overrides (e.g. exposing postgres for GUI clients) — never deploy
docker-compose.override.yml
**/docker-compose.override.yml

uploads
.idea
build
*dist